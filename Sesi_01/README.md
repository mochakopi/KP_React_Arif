# Belajar GIT

```
git config --global user.name "Nama"
git config --global user.email "Email"
```

```
buat folder dengan nama Berikut untuk di "Folder di Drive" dan "Repository"

PRAKERJA_REACT_B2_ARIF
```

```
## check all
git config --list

## inisialisasi git
git init

## remove your credentials stored in the cache
git config --global --unset credential.helper

## push data
git add .
git commit -m "sesi 01"
git push origin main

## lihat semua branch
git branch -a

## pindah branch
git checkout main

## buat branch baru
## git checkout -b landing-page


```
